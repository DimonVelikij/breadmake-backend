# Learn more about services, parameters and containers at
# http://symfony.com/doc/current/service_container.html
parameters:
    #parameter_name: value

services:
    # default configuration for services in *this* file
    _defaults:
        # automatically injects dependencies in your services
        autowire: true
        # automatically registers your services as commands, event subscribers, etc.
        autoconfigure: true
        # this means you cannot fetch services directly from the container via $container->get()
        # if you need to do this, you can override this setting on individual services
        public: false

    # makes classes in src/AppBundle available to be used as services
    # this creates a service per class whose id is the fully-qualified class name
    AppBundle\:
        resource: '../../src/AppBundle/*'
        # you can exclude directories or files
        # but if a service is unused, it's removed anyway
        exclude: '../../src/AppBundle/{Entity,Repository}'

    # controllers are imported separately to make sure they're public
    # and have a tag that allows actions to type-hint services
    AppBundle\Controller\:
        resource: '../../src/AppBundle/Controller'
        public: true
        tags: ['controller.service_arguments']

    # add more services, or override services that need manual wiring
    # AppBundle\Service\ExampleService:
    #     arguments:
    #         $someArgument: 'some_value'

    app.image_crop:
        class: AppBundle\ImageCropService\ImageCrop
        public: true
        autowire: false
        arguments:
            - '%app.save_dir%'
            - '%app.asset_dir%'
            - '%app.entities%'

    AppBundle\ImageCropService\ImageCrop: '@app.image_crop'

    app.upload_crop_file:
        class: AppBundle\ImageCropService\UploadCropFile
        public: true
        autowire: false
        arguments:
            - '@app.image_crop'
            - '@doctrine.orm.entity_manager'

    AppBundle\ImageCropService\UploadCropFile: '@app.upload_crop_file'

    app.menu_builder:
        class: AppBundle\Menu\Builder
        public: true
        autowire: false
        arguments:
            - '@knp_menu.factory'

    app.main_menu:
        class: Knp\Menu\MenuItem
        factory: ["@app.menu_builder", makeMenu]
        public: true
        autowire: false
        arguments:
            - '@doctrine.orm.entity_manager'
            - '@request_stack'
        tags:
            - { name: knp_menu.menu, alias: main }
